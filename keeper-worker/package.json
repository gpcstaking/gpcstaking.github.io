{
  "name": "gpc-oracle-keeper-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "viem": "2.55.2"
  },
  "devDependencies": {
    "wrangler": "4.92.0"
  }
}
